# packpub

📦 Package publisher

## Development

### Local

Install the modules and create build:

```zsh
pnpm install
pnpm build
```

Link the project to your global `node_modules`:

```zsh
pnpm link .
```

Run the CLI tool:

```zsh
pnpm packpub
```
